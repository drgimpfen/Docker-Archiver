services:
  app:
    volumes:
      # Development stack directories (adjust to your setup)
      - /opt/stacks:/local/stacks
      # Add more as needed:
      # - /srv/docker/stacks:/local/srvstacks
      # - /home/user/docker:/local/userdocker
